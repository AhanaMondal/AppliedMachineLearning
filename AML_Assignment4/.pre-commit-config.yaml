repos:
  - repo: local
    hooks:
      - id: pytest-coverage
        name: "pytest with coverage"
        entry: pytest tests/ --cov=. --cov-report=term-missing
        language: python
        pass_filenames: false
        additional_dependencies: ['pytest', 'pytest-cov', 'requests']